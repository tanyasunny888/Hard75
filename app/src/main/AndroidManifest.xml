<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hard75.hard75">
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />


    <!-- для Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- чтобы после перезагрузки пересоздавать будильники -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hard75">

        <!-- Экран соглашения = точка входа -->
        <activity
            android:name=".ConsentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".CreateChallengeActivity"
            android:exported="false" />

        <activity android:name=".ChallengeBoardActivity" android:exported="false"/>

        <activity
            android:name=".reminders.ReminderSettingsActivity"
            android:exported="false" />


        <!-- Напоминания: ресивер -->
        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false" />

        <!-- Ресивер на перезагрузку — пересоздать будильники -->
        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>
</manifest>
