<?xml version="1.0" encoding="utf-8"?>
<com.hard75.hard75.ui.views.SquareFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- фон-изображение с «загнутым уголком» (если используешь картинку) -->
        <ImageView
            android:id="@+id/bgImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@null"
            android:src="@drawable/sticker_style1"/>

        <!-- контент стикера -->
        <LinearLayout
            android:id="@+id/contentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="10dp"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <TextView
                android:id="@+id/tvDay"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="День 1"
                android:textStyle="bold"
                android:textSize="@dimen/sticker_day_text"
                android:textColor="#222222"
                android:layout_marginBottom="0dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"/>

            <!-- низ: круг прогресса + % -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginBottom="4dp"
                >

                <com.google.android.material.progressindicator.CircularProgressIndicator
                    android:id="@+id/progress"
                    style="@style/Widget.Material3.CircularProgressIndicator"
                    android:layout_width="@dimen/sticker_progress_size"
                    android:layout_height="@dimen/sticker_progress_size"
                    app:indicatorSize="@dimen/sticker_progress_size"
                    app:trackThickness="8dp"
                    android:indeterminate="false"
                    app:indicatorColor="@color/progress_circle_pastel"
                    app:trackColor="@color/progress_track_pastel" />

                <TextView
                    android:id="@+id/tvPercent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"/>
            </LinearLayout>
        </LinearLayout>

        <!-- большая зелёная галочка при 100% -->
        <ImageView
            android:id="@+id/ivDone"
            android:layout_width="68dp"
            android:layout_height="68dp"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            android:src="@drawable/ic_check_circle"
            android:contentDescription="@null"/>
    </FrameLayout>
</com.hard75.hard75.ui.views.SquareFrameLayout>
